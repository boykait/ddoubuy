spring:
  application:
    name: cart-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848  # 配置中心地址
        file-extension: yaml
      discovery:
        server-addr: 127.0.0.1:8848 # 服务注册中心